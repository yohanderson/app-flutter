import 'package:flutter/material.dart';

class AppsTrips extends StatelessWidget {
  const AppsTrips({super.key});

  @override
  Widget build(BuildContext context) {
    // ignore: todo
    // TODO: implement build
    return Container(
      color: Colors.yellow,
    );
  }
}